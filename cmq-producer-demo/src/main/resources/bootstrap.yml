server:
  port: 18084
spring:
  application:
    name: cmq-producer-demo
  cloud:
    stream:
      bindings:
        input:
          destination: test-topic
        output:
          destination: test-topic
      cmq:
        bindings:
          input:
            consumer:
              pollingWaitSeconds: 3
        binder:
          secretId: 
          secretKey: 
          endpoint: 